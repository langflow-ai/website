import React from "react";
import styles from "./styles.module.scss";
import TopBadge from "../icons/TopBadge";
const PrizeBadge = () => {
  return (
    <div className={styles.container}>
      <TopBadge />
    </div>
  );
};

export default PrizeBadge;
